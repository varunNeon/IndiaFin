name: Keep Streamlit Alive

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl --silent --show-error --location --max-time 30 https://indiafin-mf9orp8gbadok3wzmxppj7.streamlit.app > /dev/null
